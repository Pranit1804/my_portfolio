import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF0A0A0A); // near black
  static const surface = Color(0xFF121212); // dark card
  static const border = Color(0xFF1E1E1E);
  static const text = Color(0xFFEAEAEA);
  static const subtext = Color(0xFF9AA0A6);
  static const accent = Color(0xFF00FF88); // neon green
}
